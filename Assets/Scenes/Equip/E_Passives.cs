using UnityEngine;
using System.Collections;

public class E_Passives : MonoBehaviour {

/*	public Rect Box;
	public Rect Title;
	public Rect[] PassiveButtons;
	GUIStyle smallFont;
	E_Status selected;

	// Use this for initialization
	void Start () {
		smallFont = new GUIStyle();
		
		smallFont.fontSize = 30;
		smallFont.normal.textColor = Color.white;


	}

	void FixedUpdate()
	{
		selected = GetComponent<E_Status>();
	}

	// Update is called once per frame
	void OnGUI () {

		GUI.Box(Box,"");
		GUI.Label(Title,"Passives",smallFont);

		for (int i = 0; i < passive.passives.Count; i++)
		{
			if (PassiveButtons.Length >= i && i <= 4)
			{
				GUI.Button(PassiveButtons[i], passive.passives[i].name);
				PassiveButtons[i].width = 84.4f;
				PassiveButtons[i].y = 456f;
				PassiveButtons[i].height = 25.1f;
				PassiveButtons[i].x = 12.58f + ((PassiveButtons[i].width+2)*i);
			} else if (PassiveButtons.Length >= i && i <= 9)
			{
				GUI.Button(PassiveButtons[i], passive.passives[i].name);
				PassiveButtons[i].width = 84.4f;
				PassiveButtons[i].y = 483f;
				PassiveButtons[i].height = 25.1f;
				PassiveButtons[i].x = 12.58f + ((PassiveButtons[i].width+2)*(i-5));
			} else if (PassiveButtons.Length >= i && i <= 14)
			{
				GUI.Button(PassiveButtons[i], passive.passives[i].name);
				PassiveButtons[i].width = 84.4f;
				PassiveButtons[i].y = 510f;
				PassiveButtons[i].height = 25.1f;
				PassiveButtons[i].x = 12.58f + ((PassiveButtons[i].width+2)*(i-10));
			} else if (PassiveButtons.Length >= i && i <= 19)
			{
				GUI.Button(PassiveButtons[i], passive.passives[i].name);
				PassiveButtons[i].width = 84.4f;
				PassiveButtons[i].y = 537f;
				PassiveButtons[i].height = 25.1f;
				PassiveButtons[i].x = 12.58f + ((PassiveButtons[i].width+2)*(i-15));
			}
		}
	}*/
}
